package com.AA2project;

public class 零钱通第三版 {
    public static void main(String[] args) {
        new 零钱通第二版().A2FangFa();
    }
}
/*
入口类 new 控制类，控制类 new 数据类，数据类干活。每层只负责自己的事，通过"对象.方法()"层层调用。
三个类的职责
表格
类	职责
A1	数据 + 业务逻辑：存储零钱通的所有数据（余额、记录、数组等），提供具体操作方法
零钱通第二版	流程控制：创建 A1 对象，显示菜单，接收用户输入，调用 A1 的方法
零钱通第三版	程序入口：只负责启动，调用 零钱通第二版 的方法
零钱通第三版.main()
    ↓
new 零钱通第二版().A2FangFa()   【你起的方法名，建议改成 run() 或 start()】
    ↓
创建 A1 对象
    ↓
显示菜单 → 循环读取用户输入 → 调用 A1 的具体方法
1. 为什么 第三版 能调用 第二版？
// 因为 A2FangFa() 是 public 的非静态方法
new 零钱通第二版().A2FangFa();
// ↑创建对象          ↑调用对象的方法
非静态方法必须通过对象调用
同包的类不需要 import
2. 为什么 第二版 里能调用 A1？
A1 a1 = new A1();   // A1 和 零钱通第二版 同包，直接可用
a1.A1();            // 通过对象调用 A1 的非静态方法
a1.money = ...;     // 访问 A1 的公共字段
3. 为什么不能直接 new 零钱通第二版() 就运行？
new 零钱通第二版();   // ❌ 只创建对象，什么都不做，程序直接结束
new 零钱通第二版().A2FangFa();  // ✅ 创建对象 + 调用方法，才会执行逻辑
构造对象 ≠ 执行逻辑，必须显式调用方法。
 */